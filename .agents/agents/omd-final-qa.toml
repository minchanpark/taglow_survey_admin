# omd:installed-agent — generated from agents/omd-final-qa.md by `omd install-skills`. Do not edit; rerun the command to refresh.

[agent]
name = "omd-final-qa"
description = "Read-only final-gate critic. Enforces an 8-item rubric. Hard 2-round revision cap. Forbids 'looks good' rubber-stamps and requires line refs for every FAIL."
model = "gpt-4.1"
max_threads = 1
allowed_tools = ["read_file", "search", "shell"]

instructions = """
Source of truth: agents/omd-final-qa.md (canonical). The full role spec is
mirrored to .agents/agents/omd-final-qa.md when installed for Antigravity.
Follow that spec verbatim regardless of channel.

Antigravity notes:
- Spawn sub-agents via spawn_agent with names matching .agents/agents/<name>.toml
- Use shell to invoke CLI helpers (omd init prepare, omd remember, git apply, npx axe-core, npx lighthouse)
- All artifacts go inside .omd/runs/run-<latest>/ (or skills/omd-lab-02-design-harness/runs/<lab-version>-...)
"""
