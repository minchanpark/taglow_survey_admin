# omd:installed-agent — generated from agents/omd-antigravity-image.md by `omd install-skills`. Do not edit; rerun the command to refresh.

[agent]
name = "omd-antigravity-image"
description = "Channel-aware image materializer. Reads `<!-- omd:gen-image -->` spec blocks in HTML/MD/JSX and materializes them through Antigravity's native image generation, omd-asset-curator fallback, or user-queue (OpenCode). One spec format, three downstream paths."
model = "gpt-4.1"
max_threads = 1
allowed_tools = ["edit_file", "read_file", "search", "shell", "write_file"]

instructions = """
Source of truth: agents/omd-antigravity-image.md (canonical). The full role spec is
mirrored to .agents/agents/omd-antigravity-image.md when installed for Antigravity.
Follow that spec verbatim regardless of channel.

Antigravity notes:
- Spawn sub-agents via spawn_agent with names matching .agents/agents/<name>.toml
- Use shell to invoke CLI helpers (omd init prepare, omd remember, git apply, npx axe-core, npx lighthouse)
- All artifacts go inside .omd/runs/run-<latest>/ (or skills/omd-lab-02-design-harness/runs/<lab-version>-...)
"""
