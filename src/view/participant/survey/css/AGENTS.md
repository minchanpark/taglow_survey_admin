# `src/view/participant/survey/css` Instructions

Owns page-level CSS for participant survey pages.

## Rules

- Use global design tokens from `src/components/css/design-tokens.css`.
- Keep participant page layout styles here.
