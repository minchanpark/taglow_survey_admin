# `src/view/participant/survey` Instructions

Owns participant survey loading and rendering.

## Rules

- Use `/survey/:publicIdentifier` as the route contract.
- Load only published survey structure through the participant API boundary.
- Do not create responses or answers from preview/loading-only pages.
