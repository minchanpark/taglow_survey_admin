# `src/view/participant` Instructions

Owns participant-facing route views.

## Rules

- Views may import participant query hooks and shared components.
- Views must not import Supabase SDK, gateways, mappers, raw DTOs, or admin query hooks.
- Page-level CSS belongs in each feature `css` directory.
