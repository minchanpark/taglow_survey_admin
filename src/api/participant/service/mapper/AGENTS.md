# `src/api/participant/service/mapper` Instructions

Owns raw row to participant domain conversion.

## Rules

- Convert `snake_case` raw rows into `camelCase` models.
- Keep public route fallback rules close to participant survey mapping.
